#include "main.h"
/**
 * mul - multiply two ints
 * @a: first int to be multiplied
 * @b: second int to be multiplied
 * Return: result
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
